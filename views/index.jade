extends layout

block content
  h1.pull-left Apps to Rate
  form.form-inline.pull-right(action='/add', method='post', style='margin: 16px;')
    div.form-group(style='margin-right: 8px;')
      label.sr-only(for='url') Store Url
      input.form-control(name='url', placeholder='store url')
    input.btn.btn-default(type='submit', value='add app')
  table.table.table-hover.table-condensed
    tr
      th 
      th Title
      th Platform
      th Ratings
      th Rate It!
    each app in apps
      - var status = (app.rating > 50 ? 'success' : 'danger');
      tr(class=status)
        td.logo
          img(src=app.image, alt='logo', style='width: 50px; height: 50px; background-color: #003053;')
        td #{app.title}
        td #{app.platform}
        td #{app.rating} ratings
        td
          a(href=app.url, target='_BLANK') rate it now!

  small Windows 8 app rating numbers are updated infrequently on the website. For the most up to date information, check the built in store in Windows 8.1.